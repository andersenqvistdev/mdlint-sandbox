# Unclosed fence

A closed fence first, so the gate can also confirm it stays quiet here.

```python
print("closed")
```

An indented fence, closed with a matching indented run.

  ```text
  content
  ```

A fence that is opened but never closed before the end of the file.

```python
print("never closed")
