# Mixed fence markers

The first fence in this document uses backticks; a later fence that switches
to tildes should be flagged as inconsistent. A third fence switching back to
backticks matches the file's first marker and should not be flagged.

```python
one
```

~~~text
two
~~~

```bash
three
```
